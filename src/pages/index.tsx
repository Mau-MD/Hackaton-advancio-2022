import type { NextPage } from "next";
import { Button, Card } from "@mantine/core"

const Home: NextPage = () => {

  return (
    <div>
      adios
      <Button>Hola</Button>
    </div>
  );
};

export default Home;
